# Deprecated: Streamlit frontend was replaced by the Flask web UI.
# Use the root docker-compose.yml `web` service (backend/Dockerfile) instead.
#
#   docker compose up --build
#   open http://localhost:5000

FROM scratch
LABEL org.opencontainers.image.description="Deprecated — use backend/Dockerfile (Flask UI + API)"
