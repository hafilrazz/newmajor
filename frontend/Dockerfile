# Deprecated — Streamlit frontend was replaced by the Flask Volt UI.
# Use the unified image instead:
#
#   docker compose up --build
#   open http://localhost:5000
#
# Image is built from backend/Dockerfile (or root Dockerfile).

FROM scratch
LABEL org.opencontainers.image.description="Deprecated — use backend/Dockerfile"
